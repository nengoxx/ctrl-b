# ctrl-b Dashboard Prototype

Standalone React/Vite prototype for a future `ctrl-b` dashboard.

```powershell
npm install
npm run dev
```

Open the URL printed by Vite, usually `http://127.0.0.1:5173`.

The specification is in `docs/SPEC.md`.
