~~the bottom sheet could save the last position and reopen as it was, either fully open or jut the peeking, so if you exit leaving it fully ooen, the next one you open will be fully open, and vice versa~~ ✓ FIXED — the `<BottomSheet>` primitive gained a controlled-with-callback seam (`initialSnap` + `onSnapChange`, mirroring vaul/Radix; persistence stays the host's job). A generic KEYED store (`store/sheetSnap.ts`, `Record<id, "peek"|"full">` like `store/collapse`) persists the last detent per-device; cosmos wires it under key `"cosmos-host-detail"`. Reusable by any theme's sheet (frontier T5) via a different key — no per-theme store.




**DEFERRED (owner, 2026-06-29).** Make the cosmos service banners OPTIONAL/composable like the D30 plan-pill addon — today they're a reusable helper (`lib/assignFromSet` + the `--svc-banner`/`--svc-veil` CSS recipe) but the rendering is hardwired into `CosmosHostDetail` (always-on, no slot/flag). Two paths when wanted: (1) cheap — a cosmos `banners` on/off theme-setting (like `moonStyle`) gating the `--svc-banner` application, making it user-toggleable; (2) bigger — a shared `HostDetail` component with a banner SLOT so any theme's bottom sheet opts in by a prop (premature until a 2nd theme has a host-detail sheet, e.g. frontier — decide then, driven by real duplication, not a guess).

The vapor theme cannot use the minimal appbar mode, i also want the menu icon to mimic the rest of the themed icons like the mic icon and such



~~The icon menu sits on top of the floating sound player when tts is playing and I cant click the X button on it, any suggestions how we can solve that? maybe a shorter player to make room for the menu icon?~~ ✓ FIXED — in minimal mode the mini-player anchors left→right (not centered full-width) and leaves the top-right launcher zone (44+12px) clear, so the ✕ is always tappable (kit.css `.kit:has(.navmenu) .mini-player`).

**DEFERRED — not worth the cost for now (owner, 2026-06-28).** Big full-stack change (new persisted column + SSE wire-protocol field + store/render) for a small payoff; revisit only if the model-attribution becomes genuinely useful. Seam map kept below so it's a cheap pickup if we change our mind. — Agent chat could have a minimal model info for each response (local/cloud/local fallback/cloud fallback) next to the time.
  - Seam map (mirrors the `agent` per-message attribute): `StreamReport.served` + `.degraded` already carry "which endpoint + was it a fallback" (`adapters/inference.py`, from `core/failover.py` `FailoverResult.served_index`). The 4 states = served{local,cloud} × degraded{no,yes}.
  - Threading: NOT on `message.start` (served/degraded aren't known until `stream_chat` initiates failover) → attach to `message.end`. Persist a structured `source: {by, fallback}` (one JSON column, extends to model-name/latency later per the "shape to extend" directive — not parallel `source`/`degraded` columns). Domain `Message.source` → `db.py` migration → `MessageRepo` insert/select → SSE `message.end` → `store/chat.ts` → `ChatMessage.source` → badge in the bot who-line next to the time.

~~the tts play button on the agent chat could be slightly rounded (the corners)~~ ✓ FIXED — rounded the play-triangle corners (fill + `stroke-linejoin:round`) and the pause bars (`rx`) for both the agent-chat `.tts-play` and the floating mini-player `.mp-play` (kit.css; vapor's own icons left untouched per D7).

**DEFERRED — fix only if it recurs (owner, 2026-06-28).** Intermittent: with the composer **plan sheet open** + the **Android keyboard up** on the cosmos **Fleet** tab, a stray vertical scrollbar could appear and the composer would scroll up toward the top (half-scrolled). Could NOT reproduce on follow-up, so no speculative layout change was made (regression risk). The camera is NOT the cause and already ignores the plan sheet (`CosmosFleet.tsx` fit measures `liveBottomClosed` = `.kit-composer` top, never `.plan-sheet`). Leading suspicion: `.plan-sheet { max-height: 52vh }` uses the **layout** viewport, which the Android keyboard does NOT shrink — a long plan could then grow taller than the keyboard-shrunk visible area and spill into overflow. **Cheap hardening when it returns:** cap the sheet to the keyboard-aware `--app-h` (e.g. `max-height: min(52vh, calc(var(--app-h) - <reserve>))`) + `overscroll-behavior: contain` on `.plan-sheet` so it can never spill into a page scroll — does NOT touch the camera. Capture repro detail: number of plan steps + whether it triggered the instant the keyboard opened vs after typing.