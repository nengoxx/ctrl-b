# ISSUES — owner UX-polish backlog (ISS-#)

> **What this is.** The owner's running list of UX papercuts + polish wishes — subjective calls,
> deliberately separate from the code-audit ledgers (`UI_AUDIT.md` F# · `SYSTEM_AUDIT.md` SYS-# ·
> `AGENT_CHAT_AUDIT.md` ACA-#). Items are numbered **ISS-#** in original order (older external
> references like "ISSUES #1" = ISS-1). States: ✓ FIXED · **DEFERRED (owner)** · **OPEN**.

**ISS-1** · ~~the bottom sheet could save the last position and reopen as it was, either fully open or jut the peeking, so if you exit leaving it fully ooen, the next one you open will be fully open, and vice versa~~ ✓ FIXED — the `<BottomSheet>` primitive gained a controlled-with-callback seam (`initialSnap` + `onSnapChange`, mirroring vaul/Radix; persistence stays the host's job). A generic KEYED store (`store/sheetSnap.ts`, `Record<id, "peek"|"full">` like `store/collapse`) persists the last detent per-device; cosmos wires it under key `"cosmos-host-detail"`. Reusable by any theme's sheet (frontier T5) via a different key — no per-theme store.

**ISS-2** · ~~RULED — build path 1 (owner, 2026-08-20; was DEFERRED 2026-06-29)~~ **✓ FIXED same-day** — the `banners` switch shipped exactly as ruled (cosmos `settings` row auto-rendered in Conf · Appearance; one gate in `CosmosHostDetail` covers both service-row branches, dealt pool + owner override alike; hooks still run unconditionally). Default ON = byte-identical pre-toggle look. UI-verified on a temp instance: ON → rows carry `kit-svc-banner`, one Conf toggle → 0 of the same rows. **Owner device round PASSED 2026-08-20 (prod v1.7.4): the toggle works.** Make the cosmos service banners OPTIONAL — the rendering is hardwired into `CosmosHostDetail` (always-on, no flag). **The 2026-08-20 landscape check (the deferral's premises moved):** the "new system" now exists — the Kit Art System (D53) is the shared banner machinery, and cosmos already rides it (`useServiceBanners` + `serviceBannerProps`, DEAL THEN OVERRIDE: owner-dropped file → cosmos's bundled decorative pool → nothing). Other themes DO have the ability already: the kit Fleet (`theme-engine/kit/Fleet.tsx`) renders owner-dropped service banners for every kit theme — but with no bundled pool, so those surfaces are empty until the owner drops art (dormant by design). Gacha/frontier host-detail sheets render no service banners (frontier's "banner" is its name/status header — a different thing). So the ONLY hardwired part left is cosmos's always-on bundled pool, and the original path 2 (a shared HostDetail banner slot) died of its own test — frontier shipped a host-detail sheet and never wanted service banners, so the justifying duplication never materialized. **The ruling: path 1** — one cosmos theme-setting (`banners`, switch, **default ON**) gating the whole banner layer in cosmos's sheet (dealt pool + owner override alike), exactly the `moonStyle`/`outlines` per-theme-setting pattern (auto-rendered in Conf · Appearance). Default ON = cosmos stays byte-identical to today; OFF is the new option. Kit-Fleet owner-drop banners are untouched (already owner-controlled via media drops).

**ISS-3** · ~~The vapor theme cannot use the minimal appbar mode, i also want the menu icon to mimic the rest of the themed icons like the mic icon and such~~ ✓ FIXED — by the D51 assimilation, retroactively: the restriction lived in vapor's bespoke `AppBar` (no menu, so it fell back to `off`), which Phase 16 deleted; under `DefaultRoot` vapor gets `minimal` (the floating kit NavMenu) for free (`VaporRoot.tsx` records it). Sat mis-filed as OPEN until the owner re-raised it — **owner-verified on device 2026-08-19: "looks good"**, the kit menu icon included. If a vapor-styled menu icon is ever still wanted, that's a new ISS (theme CSS on kit hooks, allowed under the D51 freeze — the freeze covers vapor's visual identity/bespoke chrome, not theming shared kit hooks).

**ISS-4** · ~~The icon menu sits on top of the floating sound player when tts is playing and I cant click the X button on it, any suggestions how we can solve that? maybe a shorter player to make room for the menu icon?~~ ✓ FIXED — in minimal mode the mini-player anchors left→right (not centered full-width) and leaves the top-right launcher zone (44+12px) clear, so the ✕ is always tappable (kit.css `.kit:has(.navmenu) .mini-player`).

**ISS-5** · ~~RULED (owner, 2026-08-20, on the R47 menu): OPTION 1 DEFAULT + the warn color on fallback + a tap-to-reveal metrics disclosure.~~ **✓ FIXED same-day — built as D62, REVIEW-CLOSED (Emma lane blind round: SHIP WITH FIXES, 3 MED → fix wave `5359132` → confirm round all-RESOLVED, final SHIP).** The wave: error terminals now emit `_message_end` after persisting (live view = refresh); the sr-only toggle stops propagation (keyboard activation no longer double-toggles); duration stamps before the window probe (idempotent helper, tok/s truthful); + the live-drive F4 — llama.cpp-style additive cache reports (`cached > input`, impossible under subset semantics) display the honest sent-up total in the ↑ figure AND the window share, facts persisted as-reported (recorded residual: an additive report with cached < newly-prefilled is a false negative — conservative, accepted by the reviewer). As-built: the routing record is `Message.source` (`{served, degraded, from?, failed_hops?, context_window?}`) riding the `messages.meta` JSON column as one more KEY beside `usage`/`steer` — no column, no migration — and `usage` gained `cached_tokens` + `duration_ms` (the only measured field: the inference adapter times each call in `_stamp_duration`, so a message persists a duration even when the endpoint reports no counts). Every fact is read off the ONE `StreamReport` a call is given (`primary` = the chain head, `context_window` = the served target's window through the SAME `effective_window` ladder the D60 pressure gate prices against), so the single persist door `_persist_assistant` attributes every path — including the error and wrap-up calls — by construction. It is threaded live on `message.end` (`_message_end`) and durably by the ordinary thread-load dump, byte-identically. Frontend: `BotWhoLine` (`components/chatAttribution.tsx`) renders the always-on endpoint segment with a `max-width`/ellipsis guard, the `--warn` token on the segment alone when `degraded`, and the tap-anywhere disclosure (D25 BREAKOUT pattern — the line can host the read-aloud button, so the row stays a plain `<div onClick>` with an sr-only `<button aria-expanded>` as the keyboard toggle, and no visual affordance per the owner ruling); every segment omits itself when its datum is absent, so a pre-D62 message renders the old who-line and is not tappable. `failed_hops` is the ONE key added beyond the D62 shape list — the entry's own disclosure line specifies "N failed hops", and it is exactly the additive-fact growth R47 §4a described. Kit-level CSS, so every theme incl. vapor (which has had no chat CSS of its own since D51). The composite as ruled: (i) the always-on endpoint chip in the who-line (`⏺ ASSISTANT · CORSAIR · 14:32`), with an ellipsis guard so a long name can never break the 10px line; (ii) when the turn was served by a fallback, the chip takes the warn token — option 2's visual layered on top, "a more visual color when it actually switches"; (iii) option 3's tap-to-reveal grows into a small metrics disclosure (context window %, tokens, cache reuse, … — content ruling in conversation, build pending design confirm). **ALL THREE R47 options stay recorded (owner: "note them just in case — maybe another theme wants another option"):** 1 = always-on chip · 2 = exception-only warn marker (the owner likes this path; it inverts from 1 by adding one condition) · 3 = dot-color + crossfade/tap reveal. A future theme could legitimately pick a different one (per-theme fidelity, D7). Persisted shape = R47 §4a option B (`source` object beside `usage`). The payoff genuinely grew since the deferral: qwen-on-corsair is now the primary with a real fallback chain behind it, so which-endpoint-served varies turn to turn in daily use. Seam map below re-verified 2026-08-20 — `StreamReport.served`/`.degraded` live in `adapters/inference.py` and now also carry the ACA-18 prompt-cache telemetry, which strengthens the one-JSON-source-column design (room to grow model-name/latency/cache facts later). Still a full-stack slice (design-confirm before build, own review round). — Agent chat could have a minimal model info for each response (local/cloud/local fallback/cloud fallback) next to the time.
  - Seam map (mirrors the `agent` per-message attribute): `StreamReport.served` + `.degraded` already carry "which endpoint + was it a fallback" (`adapters/inference.py`, from `core/failover.py` `FailoverResult.served_index`). The 4 states = served{local,cloud} × degraded{no,yes}.
  - Threading: NOT on `message.start` (served/degraded aren't known until `stream_chat` initiates failover) → attach to `message.end`. Persist a structured `source: {by, fallback}` (one JSON column, extends to model-name/latency later per the "shape to extend" directive — not parallel `source`/`degraded` columns). Domain `Message.source` → `db.py` migration → `MessageRepo` insert/select → SSE `message.end` → `store/chat.ts` → `ChatMessage.source` → badge in the bot who-line next to the time.

**ISS-6** · ~~the tts play button on the agent chat could be slightly rounded (the corners)~~ ✓ FIXED — rounded the play-triangle corners (fill + `stroke-linejoin:round`) and the pause bars (`rx`) for both the agent-chat `.tts-play` and the floating mini-player `.mp-play` (kit.css; vapor's own icons left untouched per D7).

**ISS-7** · **✓ HARDENED (owner-requested 2026-08-20 — "do the cheap hardening if it's not overcomplicated"; was DEFERRED fix-if-recurs 2026-06-28).** The recorded two-line recipe shipped at the sheet's kit home — amended by the mini-round MED (2026-08-20): the clamp bases on the CONTAINING BLOCK, `max-height: min(52vh, calc(100% - var(--composer-h, 64px) - 48px))` (`--app-h` spans the whole shell incl. appbar/tab bar, so the first cut could still clip the sheet's top; `.kit-main`'s 100% is the visible area and shrinks with the keyboard, since the shell's height IS `--app-h`), plus gacha subtracting its own `--gc-nav-zone` lift in its existing re-anchor block + `overscroll-behavior: contain`. Risk-assessed clean: in every no-keyboard case `min()` resolves to the original 52vh (browser-verified byte-identical, 468px at 900px viewport), the clamp only engages when the visual viewport actually shrinks (post-fold measure: 430px shell → 250px under gacha = container − live composer − nav zone − clearance, sheet top inside the container), gacha now carries its OWN `max-height` override beside its `bottom` re-anchor (the one theme that lifts the sheet; no other theme re-anchors — reviewer-swept), and the e2e pins the bottom gap, not height. Residual: the original intermittent was never reproduced, so the hardening is preventive — if the symptom somehow recurs anyway, capture the repro detail below. ⚠ Honesty note (2026-08-20): the owner recalled this as "I think that was fixed" — it was NOT; no code change ever landed, it simply never reproduced after 2026-06-28. What DID change: the plan sheet is now **kit-owned** (`theme-engine/kit/kit.css` `.plan-sheet`, still `max-height: 52vh` on the layout viewport), so since the assimilation EVERY kit theme rides this geometry, not just cosmos — the recorded cheap hardening below applies unchanged at the new home, just with a wider blast radius. Intermittent: with the composer **plan sheet open** + the **Android keyboard up** on the cosmos **Fleet** tab, a stray vertical scrollbar could appear and the composer would scroll up toward the top (half-scrolled). Could NOT reproduce on follow-up, so no speculative layout change was made (regression risk). The camera is NOT the cause and already ignores the plan sheet (`CosmosFleet.tsx` fit measures `liveBottomClosed` = `.kit-composer` top, never `.plan-sheet`). Leading suspicion: `.plan-sheet { max-height: 52vh }` uses the **layout** viewport, which the Android keyboard does NOT shrink — a long plan could then grow taller than the keyboard-shrunk visible area and spill into overflow. **Cheap hardening when it returns:** cap the sheet to the keyboard-aware `--app-h` (e.g. `max-height: min(52vh, calc(var(--app-h) - <reserve>))`) + `overscroll-behavior: contain` on `.plan-sheet` so it can never spill into a page scroll — does NOT touch the camera. Capture repro detail: number of plan steps + whether it triggered the instant the keyboard opened vs after typing.

**ISS-8** · ~~OPEN (owner, 2026-08-20)~~ **✓ FIXED same-day** — one kit rule (`.kit .b.user .who { flex-direction: row-reverse }`): the row packs to the bubble's right edge and the ::before dot lands outermost-right, the exact mirror of the assistant's dot-first left. UI-verified flush (0px gap to the bubble edge). ⚠ Scope correction (mini-round LOW, 2026-08-20): the original "vapor untouched — no `.kit` marker" claim was STALE pre-D51 knowledge — vapor renders through DefaultRoot's `.kit` shell and ships no chat CSS of its own, so it receives this rule like every kit theme (reviewer-verified coherent there; the same right-edge treatment). **Owner prod round 2026-08-20 raised no complaint ("tried them all — they work"); treated as verified.** Agent-chat who-lines should sit on the bubble EDGES: the assistant's at the top-left (already true — the assistant bubble is full-flow left) and the user's at the top-RIGHT of its bubble. Today `.kit .b.user` is a shrink-wrapped right-aligned flex child whose `.who` row is left-anchored, so "you · time" reads mid-screen instead of hugging the right edge. Fix sketch (kit-level, so every kit theme — vapor included via DefaultRoot's `.kit`, per the scope correction above): right-align the user who-line (`justify-content: flex-end` on `.kit .b.user .who`, dot placement kept sane — likely dot AFTER the text on the user side so the marker stays outermost, mirroring the assistant's dot-first). One CSS slice + an eyeball; pairs naturally with ISS-5's badge (which lands in the same who-line).

**ISS-9** · ~~the spinning animation after the mic finishes recording (the transcribe spinner) is too minimal — I want it actually visible that the recording finished and is being worked on~~ ✓ FIXED same-day (2026-08-21) — the state had motion but no chrome: the 8-bar roll (steps(8), §14.11-safe) rendered in the button's RESTING color — muted `--text-2` in stacked, the resting accent in sheet — so nothing but the small glyph swap said "busy". One kit rule gives `.kit-cbtn.mic.sending` the `.rec` treatment's shape in the accent channel: accent glyph + `color-mix` 15% accent chip + 45% accent border — the state language is now red = recording · accent chip + roll = transcribing. The line layout's mic (already a filled accent circle, the loudest face) outspecifies the rule and keeps its look; reduced-motion keeps the static chip + spinner glyph (the existing gate). Browser-verified on dev: the base face goes from a bare muted glyph to an unmistakable accent chip. Token-only, every kit theme inherits it (vapor included via DefaultRoot). **Round 2 (owner device round 2026-08-21):** the chip landed but the roll itself was still invisible — the 8 equal-weight bars under the 45°-step rotation put every animation frame on a pixel-identical image (owner: "looks the same from all angles"). Fixed `52510fe`: graded opacity tail on the glyph (bright head fading counter-clockwise, the iOS/Material segmented-spinner pattern), so each step visibly advances the head; the static face (reduced motion, line layout) still reads as a spinner. **Owner-confirmed same day: "looks much better."**

**ISS-11** · ~~the gacha oracle's scanlines pulse a fade every ~7-10s at rest~~ **✓ FIXED same-day (owner ruled option (a): "I just need them to fade when you scroll down like the image")** — the always-on `.gc-oracle-scan` mask rule DELETED (one rule): scanlines now end crisp at rest and dissolve only with the ghosting-gated block mask, which composites the whole subtree — this supersedes the 2026-08-06 "softened comb at rest" preference and also ends the double-masking over-dim while ghosted (Emma's bonus finding). **Round 2 same-day (owner, after eyeballing the fix): a THIN at-rest melt returned, sized to the strip under the plate's JP subtitle** — built pulse-proof via the stationary-frame redesign (Emma's ISS-11 variant: `.gc-oracle-scan` never moves and holds the mask; the comb + 7s travel live on its `::before`, so the band cannot ride the animation by construction). New tunables `--gc-scan-edge-fade: 14px` / `--gc-scan-edge-mask` (the plate sits 16px off the image bottom; one number to retune). The melt is now mode-unconditional (simpler; `scroll` mode gets the same 14px courtesy). Original record below for provenance. Symptom (owner, at the top of the chat with the operator art fully visible): "every ten seconds or so there's a fade-out animation on the scan lines — the bottom-border fade-out should only happen once, on scroll, fading with the image into the background." Diagnosis (main-seat code read 2026-08-21; **CONFIRMED same day by an Emma-lane BLIND round** — from the symptom + code alone she derived the identical mechanism, element, geometry, and period before seeing the hypothesis, including why the pattern looks stationary while only the alpha pulses: the 28px travel = exactly 7 periods of the 4px comb): in fade mode, the ① comb-dissolve mask (`--gc-oracle-edge-mask`, a static bottom-48px alpha ramp) is applied to `.gc-oracle-scan` itself (gacha.css:4195) — but that element is the ANIMATED layer: the M6 loop translates it down `--gc-scan-travel` (28px) every 7s (gacha.css:4111) and a mask is painted in the element's own box, so the fade band RIDES the travel — sliding 28px down over each cycle, weakening the visible bottom dissolve, then snapping back at the loop restart. The 2026-08-06 bottom-dissolve design (the long §4170 comment) reasoned correctly about blend order but missed that the mask's host translates. Fix direction (not built — owner-parked "for later"; TWO options, the choice is an OWNER RULING because it supersedes the 2026-08-06 "softened comb at rest" preference): **(a)** if the new wish (scanlines fade only with the scroll ghosting) replaces the old one — DELETE the `.gc-oracle-scan` mask rule (:4195-4198), one deletion: the ghosting-gated block mask (:4199) already composites the whole subtree, scanlines included, so they dissolve with the image exactly as the owner describes; **(b)** if the softened comb at rest must stay — make `.gc-oracle-scan` itself the STATIONARY masked host and move the comb + 7s travel onto its `::before` (no new markup; masks composite the subtree, the band stays put while the comb slides beneath). Emma's bonus finding, fold into the same slice: once `[data-gc-ghosting]` is stamped the scanlines are today masked TWICE (their own mask × the ancestor block mask — the alpha ramps multiply), so option (a) also fixes an existing over-dimming. ② (the art edge's ghosting-gated mask on `.gc-oracle`, :4199) is NOT itself affected — that host doesn't animate.

**ISS-10** · **OPEN — stage ① SHIPPED, stage ② owner-parked (2026-08-21 rulings)** — ① the motion-token consistency pass shipped same-day (`8463082`: the two-and-two band in the semantic contract, 33 kit rules retokenized, one reduced-motion collapse in `@layer axes`, the ease→M3-standard feel swap; Emma round SHIP zero findings). ② the glyph-swap cross-fades stay UNBUILT by the owner's "if nobody does it, we don't need it either" (R52: 6/6 peers cut instantly; the polish-lineage recipe — stacked glyphs, scale 0.7 + opacity, the send-nudge refactor — stays recorded in R52 §8.2 for a one-word revival). Original wish: composer icon swaps could carry a small transition animation, like the one that already plays when the send button joins the mic (mic-only → mic+send). Today the state-driven glyph swaps (mic → spinner, send → stop, …) cut instantly; the wish is the same micro-transition language on those changes. Note for the eventual slice: the existing mic→mic+send motion is a LAYOUT animation (a second button entering the row), while these are in-place glyph swaps — a cross-fade/scale on the glyph inside the button (transform/opacity-only, §14.11; reduced-motion gate) is the likely shape.

**ISS-12** · **OPEN (main-seat finding, OF-2 sweep 2026-09-07 — needs an owner look ruling)** — under **frontier**, the modal-footer SAVE button (machine editor + the agents detail form) renders as a flat `--surface-2` chip, visually identical to Cancel — no accent fill, so the primary action doesn't read as primary. Diagnosis: frontier's border-off sweep (`frontier.css:1136-1142`) lists `.mconf .mfoot button` unqualified in a `@layer theme` rule whose own comment says it targets *"the fill-less ones"* — but `.save` IS filled (kit gives it `--accent-fill`), and the theme layer wins regardless of specificity, so the accent fill is replaced by `--surface-2` as collateral. The same rule is why frontier was immune to the OF-2 gradient-rim class on this button (the flatten masks it). Fix direction if ruled: exclude the save (`.mconf .mfoot button:not(.save)`) so kit's accent fill + the OF-2 `background-clip: padding-box` (already in place, `9f6b597`) take over — one token; but it changes frontier's shipped look, so it waits for the owner's word. Found during the OF-2 sweep; deliberately NOT folded into that wave (bug fixes stay minimal).
